<section class="main w-4/5 border border-solid border-black p-10 rounded-lg">
  <div class="flex justify-between mb-10">
    <h1 class="font-bold text-3xl"><%=title%></h1>
    <%- include("../partials/button", {text: "+ Add", url: "genres/create"}) %>
  </div>
  <div class="grid grid-cols-1 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 gap-4 ">

    <% genres.map((genre) => { %>
    <%- include('../partials/detail_button', {text: genre.name, quantity: genre.game_count, url: `/genres/${genre.id}`}) %>
    <% }) %>

  </div>
</section>